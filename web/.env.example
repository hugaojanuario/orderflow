# A URL da API é configurada em RUNTIME (não em build time):
# o frontend lê window.ENV.API_URL de /env.js.
# - Desenvolvimento local: edite web/public/env.js
# - Produção: o entrypoint do container deve gerar o env.js a partir desta variável
API_URL=http://localhost:8080
